<main>
  <div class='container'>
    <div class='row'>
      <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
        <h2>New Room</h2>
        <%= form_with(model: @room, local: true) do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group"><label for="inputTitle">Title</label>
    <%= f.text_field :title, autofocus: true, id:"Title", class: "form-control title" %>
  </div>
  <div class="form-group"> <label for="inputImage">Image</label>
    <%= f.attachment_field :image, class: "form-control-file image" %>
  </div>
  <div class="form-group"> <label for="inputBody">Body</label>
    <%= f.text_area :body, class: "form-control body" %>
  </div>

  <div class="form-group">
  <%= f.submit "Create Room", class: 'btn btn-secondary' %>
          </div>
<% end %>
<%= link_to 'Show', @room %> |
<%= link_to 'Back', rooms_path %>

          </div>
      </div>
    </div>
  </div>
</main>