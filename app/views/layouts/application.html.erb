<!DOCTYPE html>
<html>
  <head>
    <title>RoomMake</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

<body>
  <header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <a class="navbar-brand" href="/"><span>RoomMake</span></a>
          <ul class="navbar-nav ml-auto">
            <% if user_signed_in? %>
              <li>
                <%= link_to user_path(current_user.id), class: 'nav-link fas fa-home nav-link text-light' do %> マイページ
                  <% end %>
              </li>
              <li>
                <%= link_to users_path, class: 'nav-link fas fa-users nav-link text-light' do %> ユーザー
                  <% end %>
              </li>
              <li>
                <%= link_to rooms_path, class: 'nav-link fas fa-book-open nav-link text-light' do %> お部屋
                  <% end %>
              </li>
              <li>
                <%= link_to destroy_user_session_path, method: :delete, class: 'nav-link fas fa-sign-out-alt nav-link text-light' do %> ログアウト
                  <% end %>
              </li>
            <% else %>
              <li>
                <%= link_to root_path, class: 'nav-link text-white fas fa-home nav-link text-light' do %> ホーム
                  <% end %>
              </li>
              <li>
                <%= link_to home_about_path, class: 'nav-link text-white fas fa-link nav-link text-light' do %> 概要
                  <% end %>
              </li>
              <li>
                <%= link_to new_user_registration_path, class: 'nav-link text-white fas fa-user-plus nav-link text-light' do %> 新規登録
                  <% end %>
              </li>
              <li>
                <%= link_to new_user_session_path, class: 'nav-link text-white fas fa-sign-in-alt nav-link text-light' do %> ログイン
                  <% end %>
              </li>
              <% end %>
          </ul>
        </div>
      </div>
    </nav>
  </header>

  <main>
   <%= yield %>
  </main>
  <footer>
    <div class='container mt-5'>
      <div class='row'>
        <div class='mx-auto'>
          <p>CopyRight Yasu</p>
        </div>
      </div>
    </div>
  </footer>
  </body>
</html>